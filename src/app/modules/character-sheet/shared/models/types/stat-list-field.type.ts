import { StatisticDetails } from "../../../models/classes/statistic-details.class"
import { CharacterStats } from "../../../models/classes/character-stats.class";

export type StatListField = {
    index: string,
    value: CharacterStats;
}